- content_for :header_tags do
  %script#initial-state{ type: 'application/json' }!= json_escape(render(file: 'home/initial_state', formats: :json))

- content_for :content do
  .app-holder#mastodon{ data: { props: Oj.dump(default_props) } }

    -# These loads fonts required for music videos.
    %div{ style: 'font-family: Roboto; font-weight: 500' } Loading…
    %div{ style: 'font-family: Roboto; font-weight: normal' } Please wait for a moment.

    = javascript_pack_tag 'application', integrity: true, crossorigin: 'anonymous'
    %noscript
      = image_tag asset_pack_path('logo.svg')
      %div
        = t('errors.noscript')

= render template: 'layouts/application'
